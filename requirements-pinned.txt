# The exact stack the club runs on.
#
# THIS SET IS DICTATED BY COLAB. Members cannot change Colab's Python version
# or its preinstalled numpy/pandas, so those are fixed points and everything
# else is chosen to fit around them. Colab currently ships:
#
#     Python 3.12  ·  numpy 2.0.2  ·  pandas 2.2.2
#
# numpy and pandas are pinned to exactly Colab's versions so that installing
# the club package does NOT upgrade them. That matters: an upgrade forces a
# "Restart session" and a re-run, which is precisely the friction a first
# session cannot afford.
#
# vectorbt 1.0.0 is the newest release that resolves cleanly against numpy
# 2.0.2 / pandas 2.2.2. Do not "upgrade" to 1.1.0 - it drags numpy 2.4 and
# pandas 3.0 in behind it and breaks the no-restart property above.
#
# Before bumping ANYTHING here, run notebooks/smoke_test.py on a throwaway
# Colab runtime. A version that works on a laptop but not in Colab is a
# version that does not work.

numpy==2.0.2
pandas==2.2.2
pyarrow==16.1.0
requests==2.32.3
PyYAML==6.0.2

yfinance==1.5.2
ccxt==4.3.74

vectorbt==1.0.0
backtrader==1.9.78.123

google-api-python-client==2.137.0
google-auth==2.32.0

jupytext==1.16.2
